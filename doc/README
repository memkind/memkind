# doc files

This directory contains documentation files for memkind interfaces in
a human readable format (using Markdown syntax).

To see generated manpages (out of these docs), please see [man directory](../man/).

All changes in public interfaces/API should be followed by changes in
documentation files in this directory.
